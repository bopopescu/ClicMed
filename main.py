import settings

# ---------------------------------------------------------------------------------------------------------------------
# Main execution of program

settings.login.login_window(settings.root)
settings.root.mainloop()
